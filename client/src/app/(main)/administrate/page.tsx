"use client";
import React from "react";

window.location.replace('http://localhost:3000/administrate/users');

const Administrate = () => {
  return (
    <div className="flex h-full w-full flex-col overflow-hidden">
      Not a valid administration page, you will be redirected propmtly...
    </div>
  );
};

export default Administrate;
